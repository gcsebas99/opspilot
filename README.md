# OpsPilot

An incident triage agent for a fake e-commerce stack ("ShopStack"), built as a learning +
portfolio project for **harness engineering**: the loop, tools, context, environment, memory,
observability, guardrails, human-in-the-loop, permissions, audit, and evals around an LLM agent.

An alert arrives → the agent investigates a sandboxed, seeded copy of ShopStack → finds the root
cause → proposes/applies a fix (destructive actions need approval) → submits a report.

See [`docs/PLAN.md`](docs/PLAN.md) for the full design and [`docs/specs/`](docs/specs) for the
day-by-day build spec. Concepts implemented along the way are tagged `[HARNESS:*]` in the code and
indexed in `CONCEPTS.md` (generated by `scripts/concepts.py`).

## Setup

```bash
uv sync
cp .env.example .env   # fill in ANTHROPIC_API_KEY
```

## Usage

```bash
uv run opspilot --help
uv run opspilot run --scenario checkout_pool_exhaustion --seed 42
```

## Development

```bash
uv run pytest -q
uv run ruff check . && uv run ruff format --check .
uv run mypy opspilot
```
